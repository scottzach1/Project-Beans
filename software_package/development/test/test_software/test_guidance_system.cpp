#include "unity.h"

void setup(void) {
  // set up to perform before every test
}

void teardown(void) {
  // clean up to perform after every test
}

int main(void) {
  UNITY_BEGIN();
  return UNITY_END();
}
