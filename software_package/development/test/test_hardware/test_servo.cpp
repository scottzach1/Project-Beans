#include "unity.h"

void setup(void) {
  // set up to perform before every test
}

void teardown(void) {
  // clean up to perform after every test
}

int main() {
  UNITY_BEGIN();
  // RUN_TEST calls here...
  return UNITY_END();
}
